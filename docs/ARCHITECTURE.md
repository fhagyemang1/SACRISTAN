# SACRISTAN — Architecture & Stack Decision

## 1. Tech stack decision: Flutter

**Chosen stack: Flutter (Dart), targeting iOS, Android, and Windows desktop from one codebase.**

### Why Flutter over the alternatives

| Criterion | Flutter | .NET MAUI | React Native (+ RN Windows) |
|---|---|---|---|
| Offline local-database maturity | `sqlite3`/`sqflite` and `drift` (type-safe SQL, migrations, reactive streams) are mature, widely used, offline-first by design | SQLite via `sqlite-net-pcl`/EF Core is workable but EF Core's mobile story has more rough edges historically | SQLite via `op-sqlite`/`WatermelonDB`; solid, but RN Windows is a separate, less-maintained fork with lagging parity |
| Single-codebase parity across iOS/Android/Windows | Windows desktop is a first-class, actively maintained Flutter embedder (same as iOS/Android); genuinely one UI codebase | MAUI officially supports Windows (via WinUI) plus iOS/Android from one codebase — also strong here | React Native's Windows target (`react-native-windows`) is community-maintained by Microsoft but historically trails the core RN release cadence, and desktop layout/gesture parity is weaker |
| App-store review track record | Huge installed base of shipped Flutter apps on both stores; Apple/Google review teams are very familiar with Flutter's binary signature (no known systemic rejection patterns) | Good track record, smaller sample size than Flutter/RN | Good track record, but RN's JS-bridge architecture has occasionally drawn extra scrutiny for apps that dynamically load JS bundles (not our case, since we ship a static bundle) |
| Long-term maintainability by a small/volunteer team | One language (Dart) for all UI and business logic, one widget system, one styling model, hot reload for fast iteration, huge Stack Overflow/pub.dev ecosystem | C#/XAML — very approachable for teams with .NET background, but the Windows-first WinUI styling model doesn't always transfer cleanly to mobile idioms | JS/TS is the most widely known language, but bridging native modules (notifications, SMS, contacts) usually still requires touching native iOS/Android code, which raises the bar for a volunteer team |
| Accessibility (VoiceOver/TalkBack/Narrator) | Built-in `Semantics` widget tree maps directly to all three platforms' accessibility APIs | Strong, native accessibility tree | Strong, native accessibility tree, but a bit more manual wiring per platform historically |
| Package ecosystem for our specific needs (local notifications, SQLite, PDF/CSV export, SMS deep-links) | `flutter_local_notifications`, `drift`/`sqflite`, `pdf`/`csv`, `url_launcher` (for `sms:` deep links) are all mature, actively maintained, offline-capable | Equivalent MAUI packages exist but are less battle-tested in combination | Equivalent RN packages exist; SMS/notifications require native linking per platform |

**Verdict:** Flutter wins on the two criteria that matter most for this project — offline-database maturity and true single-codebase parity across all three required targets — while remaining the most learnable option for a small or volunteer team (one language, one widget model, excellent docs). This is the stack used for everything below.

**Local data layer:** [`drift`](https://drift.simonbinder.eu/) on top of `sqlite3_flutter_libs`/`sqflite`, giving compile-time-checked SQL, streaming queries (so the UI updates live as checklists are ticked), and a straightforward migration path. The bundled liturgical calendar dataset ships as a versioned Dart const dataset (not a database table) inside the pure-Dart `liturgical_calendar` package described below, so it can be improved in a future app release without any migration of user data.

**Package split:** the liturgical calendar engine lives in its own pure-Dart package (`packages/liturgical_calendar`) with **no Flutter dependency at all**. This is a deliberate architecture choice, not just tidiness:

- It can be unit-tested with the plain `dart test` runner — no emulator, simulator, or even Flutter SDK required — which is what makes the "20-year span of known correct dates" requirement actually verifiable in CI and in constrained environments.
- It is reusable outside the app (e.g. a future companion CLI or web reference tool) without dragging in Flutter.
- It keeps the hardest, most safety-critical logic (getting Easter and its dependent dates *right*) isolated from UI churn.

**Future cloud sync:** the `app/lib/data` layer talks to `drift` through repository classes (`ChecklistRepository`, `InventoryRepository`, …), never directly from UI code. A future optional sync feature would add a `SyncEngine` that watches the same repositories' change streams and pushes/pulls through a pluggable `SyncBackend` interface — no rewrite of screens or the local schema required. This is documented but **not implemented** in this build, per the offline-first requirement.

## 2. Data model

### 2.1 Calendar engine domain (pure Dart, no database — computed/looked up on demand)

```
LiturgicalDay
├─ date: DateTime (local, midnight)
├─ season: LiturgicalSeason (advent | christmas | ordinaryTime | lent | triduum | easter)
├─ weekOfSeason: int
├─ color: LiturgicalColor (violet | white | red | green | rose | gold | black)
├─ rank: CelebrationRank (solemnity | feast | memorial | optionalMemorial | sunday | ferial)
├─ celebrations: List<Celebration>   // can be >1 (e.g. optional memorial alongside a ferial)
├─ sundayCycle: SundayCycle? (A | B | C)   // set on Sundays and solemnities that borrow the Sunday readings
└─ weekdayCycle: WeekdayCycle? (I | II)    // set on ferial weekdays

Celebration
├─ name: String                 // localized via l10n keys, e.g. "stJosephTheWorker"
├─ latinName: String?
├─ rank: CelebrationRank
├─ color: LiturgicalColor
├─ isProper: bool               // true if from a local/diocesan supplementary calendar
└─ source: CalendarSource (generalRomanCalendar | localSupplement)

LocalCalendarEntry (user-editable, imported/edited fully offline)
├─ id, month, day (or movable-date rule for a handful of cases), name, latinName, rank, color, notes
└─ stored in the drift database (`local_calendar_entries` table), layered on top of the bundled GRC at resolution time
```

### 2.2 App/user data (SQLite via `drift`, `app/lib/data/database.dart`)

```
profiles            id, display_name, role (admin | volunteer), pin_hash?, locale, created_at
parishes            id, name, address, timezone_note
suppliers           id, name, contact_name, phone, notes
masses              id, date, label ("8:00 AM Sunday Mass"), mass_type (sunday|weekday|funeral|
                        wedding|baptism|benediction|holyWeek), celebrant_name?, notes
checklist_templates id, name, mass_type, phase (pre|post), is_builtin, locale
checklist_items     id, template_id, sort_order, label_key, label_custom?, latin_term?
checklist_instances id, mass_id, template_id, created_at
checklist_ticks     id, instance_id, item_id, is_done, done_at, done_by_profile_id
inventory_items     id, category (chasuble|stole|alb|linen|vessel|candle|incense|hosts|wine|other),
                        name, color, condition, storage_location, quantity, low_stock_threshold,
                        low_stock_flag, notes
notes               id, linked_type (date|mass|checklist_item), linked_id, body, created_at
reminders           id, title, body, trigger_at, repeat_rule?, linked_note_id?, is_active
contacts            id, role (pastor|sacristan|serverLeader), name, phone
reference_entries   id, category (girmExcerpt|glossaryTerm|rubricNote), title, body_md,
                        source_citation, illustration_asset?
app_settings        key, value   // locale, theme, admin_pin_hash, etc.
```

All tables are created and versioned through `drift`'s schema migration system (`schemaVersion`, `MigrationStrategy`), so future columns/tables (e.g. a `sync_state` column) can be added without destructive migrations.

### 2.3 Why the calendar engine is *not* in SQLite

Feast dates depend on Easter, which depends on the year, which is unbounded ("years in the future" is an explicit requirement). Storing a flat table of dates would mean either shipping a finite pre-computed table (breaks for far-future years, or is enormous) or recomputing anyway. Instead the engine computes `LiturgicalDay` objects on demand for any `DateTime`, in well under a millisecond, and the *only* things persisted to SQLite are (a) the small, versioned local-supplement table a parish edits, and (b) references from user data (masses, notes) to plain `DateTime`s — never to a precomputed feast-date table.

## 3. Repository layout

```
SACRISTAN/
├── docs/
│   ├── ARCHITECTURE.md            (this file)
│   └── STORE_CHECKLIST.md
├── packages/
│   └── liturgical_calendar/       (pure Dart, zero Flutter dependency)
│       ├── lib/
│       │   ├── liturgical_calendar.dart      (public API barrel)
│       │   └── src/
│       │       ├── models.dart               (LiturgicalDay, Celebration, enums)
│       │       ├── computus.dart              (Easter date algorithm)
│       │       ├── season_engine.dart         (season/week/color/rank resolution)
│       │       ├── cycles.dart                 (Sunday A/B/C, weekday I/II)
│       │       ├── general_roman_calendar.dart (bundled GRC fixed-date dataset)
│       │       └── local_calendar.dart         (merges an optional local supplement)
│       ├── test/
│       │   └── calendar_engine_test.dart
│       └── pubspec.yaml
├── app/                            (Flutter app — iOS + Android + Windows)
│   ├── pubspec.yaml
│   ├── lib/
│   │   ├── main.dart
│   │   ├── theme/app_theme.dart
│   │   ├── l10n/ (en, fr, es .arb files)
│   │   ├── data/ (drift database + repositories)
│   │   └── features/
│   │       ├── dashboard/
│   │       ├── calendar_detail/
│   │       ├── checklists/
│   │       ├── inventory/
│   │       ├── reference/
│   │       ├── notes/
│   │       ├── settings/
│   │       └── common/ (shared widgets: big touch-target checkbox, color chip, etc.)
│   └── assets/reference/ (original SVG line-art)
├── tools/verification/             (Python cross-check script used only during development,
│                                      not shipped in the app — see README)
└── README.md
```

## 4. Build-depth note for this delivery

**Round 1** (initial MVP+ scope): the calendar engine, data layer, dashboard, calendar-detail, and checklist screens fully implemented and functional against the local database. Inventory, reference library, notes/reminders, and settings screens scaffolded with working navigation, models, and sample data.

**Round 2** (this pass): filled in most of the round-1 scaffolding —
- **Local calendar editor** (Settings → Parish/Diocesan Calendar): add/delete offline supplementary-calendar entries, live on the Dashboard/Calendar immediately.
- **Admin PIN gate** (`data/admin_session.dart`, `features/settings/admin_pin_screen.dart`): salted-hash PIN (never stored in plaintext), in-memory unlock per app session, gating Inventory edit/delete as a worked example of the pattern — apply the same `AdminSession.isUnlocked` check to any other admin-only action.
- **Inventory edit/delete** and a **low-stock CSV shopping-list export** (`share_plus` hands the file to whatever app the user picks — Mail, Messages, Files — no bundled server).
- **Contacts & Suppliers** screen, plus a **"text a low-stock alert"** action on Inventory: both use `url_launcher`'s `sms:` deep link to open the device's own Messages app pre-filled — not an SMS gateway integration, no account or API key involved.
- **Reminders**, scheduled through `flutter_local_notifications` on Android/iOS/macOS/Linux. **Known gap: Windows has no native notification backend in that package** — reminders still save and list correctly on Windows, they just can't pop a system toast there; see the doc comment in `data/notifications_service.dart` for the swap-in fix (`local_notifier`) if that matters for your parish's desktop use.

Reference library content and locale coverage (beyond the en/fr/es strings already shipped) remain the same additive next-step work described in round 1.

**Round 3** (this pass):
- **Reference Library is now DB-backed**: bundled glossary/GIRM/rubric entries seed into the `ReferenceEntries` table on first launch; a parish can add its own entries (admin-gated) or delete any entry — the static lists in `reference_data.dart` are now only the seed source, not the runtime source of truth.
- **Language picker** (Settings → Language): persists the chosen locale to `AppSettings` via a new `LocaleController`, applied through `MaterialApp.locale` — switching is immediate and survives an app restart.
- **Multi-Mass support for checklists**: tapping a template now looks up today's existing Masses of that type via the new `MassRepository`; with zero or one Mass it proceeds immediately (no extra tap for the common single-Mass-parish case), with more than one it prompts "Which Mass?" (including "New Mass…"). `ChecklistRepository.findOrCreateInstance` also fixes a real bug from round 1/2: re-opening a checklist previously always created a brand-new blank instance, silently orphaning prior progress — it now resumes the existing instance for that (Mass, template) pair.
- **Bug fix, later found to be wrong (see round 9)**: several database queries (`repositories.dart`, `admin_pin_screen.dart`) called `.equalsValue()`, believed at the time not to be part of drift's real API, and were "corrected" to `.equals()` in this pass. **Round 9's real CI run proved this backward**: for a drift type-converted column (`textEnum<T>()`, used here for `MassType`/`ProfileRole`), `.equalsValue(D value)` is the real, documented method for comparing against the converted Dart/enum value, while `.equals(S value)` compares against the raw stored SQL type (`String`) — the opposite of what round 3 assumed. This round's edit replaced correct code with code that compiles but silently never matches (`equals` on a `String` parameter fed an enum would have been a compile error even then under a real analyzer — this bug could only have persisted because no compiler ever checked it until round 9). Left uncorrected here as a historical record; round 9's entry below has the actual fix. The lesson: a manual-review "fix" is only as trustworthy as the API knowledge behind it, and this project had no way to verify that knowledge until real CI existed.
- **Second attempt at installing the Dart SDK**: the sandbox's egress allowlist blocks `pub.dev`, `dart.dev`, and the GitHub releases API outright (not just the storage host tried in round 1), confirming this is a deliberate policy exclusion rather than one blocked host. No further attempts are likely to succeed from this environment; the Python cross-check from round 1 remains the standing executed verification of the calendar engine, and manual review remains the verification method for the Flutter/drift code — **run `dart test` and `flutter analyze` yourself before relying on this in production.**

**Round 4** (this pass): a full line-by-line manual review of every file under `app/lib`, cross-checking each repository method's signature against every call site (still the only verification method available — see above; the Dart/Flutter SDK remains unavailable in this sandbox). Findings, most significant first:
- **User-visible bug fixed: built-in checklist item text was never displayed.** `main.dart`'s `seedBuiltinTemplatesIfNeeded()` stored each built-in item's `labelKey` (a machine key, e.g. `"missalRibbons"`) and `latinTerm`, but never stored `item.label` — the actual authored guidance text (e.g. *"Missal and ribbons set to today's readings"*). `checklist_detail_screen.dart`'s fallback for a missing localization silently prettified the raw key into Title Case instead (`"Missal Ribbons"`), so every built-in checklist across all seven Mass types was showing crude derived labels rather than the real guidance that was written for them. Fixed by adding `labelCustom: Value(item.label)` to the seeding insert — `labelCustom` already took display priority over the key fallback, so the real text now shows immediately with no other code changes needed.
- **Version-constraint bug fixed**: the UI (`app_theme.dart` and three other files) uses `Color.withValues(alpha: ...)`, a Flutter 3.27+ API, but `pubspec.yaml` declared a Flutter 3.22+ minimum — meaning a build against the stated minimum could fail to resolve that method. Fixed by raising `environment.flutter` to `>=3.27.0` and `environment.sdk` to `>=3.6.0 <4.0.0`.
- **Two smaller defensive fixes in `admin_pin_screen.dart`**: an immediately-invoked async function expression was rewritten with explicit parentheses (`await (() async {...})()`) for unambiguous parsing rather than relying on Dart accepting the unparenthesized form; and `_adminProfile` is now updated locally right after a new admin profile/PIN is created, so the screen's `_hasPin` check stays correct even if a future edit removes the immediate `Navigator.pop()`.
- **Everything else reviewed clean**: `database.dart`, `repositories.dart` (all thirteen repository classes, cross-checked against every screen that calls them), `dashboard_screen.dart`, `calendar_detail_screen.dart`, `app_shell.dart`, `color_chip.dart`, `big_checkbox_tile.dart`, `sms_helper.dart`, `notes_screen.dart`, `inventory_screen.dart`, `reference_screen.dart` + `reference_data.dart`, `contacts_suppliers_screen.dart`, `local_calendar_editor_screen.dart`, `profiles_screen.dart`, `reminders_screen.dart`, `language_screen.dart`, `settings_screen.dart`, `checklist_list_screen.dart`, `builtin_templates.dart`, `locale_controller.dart`, `admin_session.dart`, and `notifications_service.dart` — no further bugs found. Notably, the `DropdownButtonFormField(initialValue: ...)` pattern used across six screens is applied consistently everywhere in the app (no screen still uses the older `value:` parameter), so if that pattern needs adjusting for a specific Flutter version it is a single, uniform find-and-replace.
- As in round 3: this pass is manual review, not compilation. **Run `dart test`, `flutter analyze`, and `flutter run` yourself before relying on this in production** — that remains the one step this sandbox cannot perform for you.

**Round 5** (this pass): a documentation-vs-code discrepancy check found and fixed a second genuinely user-visible gap, on top of continuing the review pattern established in round 4.
- **Localization was claimed but not actually wired.** `app_en.arb`/`app_fr.arb`/`app_es.arb` have shipped since round 1 with translations for navigation labels, liturgical color/rank/season vocabulary, and a few other strings, and a stray code comment in `checklist_detail_screen.dart` claimed this was "wired through AppLocalizations for the primary navigation and dashboard strings." It wasn't — a repo-wide search found exactly zero call sites using `AppLocalizations` outside that one comment. The Language picker (Settings → Language) changed `MaterialApp.locale`, which affects built-in Material widgets (date pickers, etc.) but none of the app's own text — switching to French or Spanish visibly changed nothing. **Fixed**: `color_chip.dart`'s `rankLabel`/`seasonLabel`/color-name functions, `app_shell.dart`'s bottom-nav labels and app-bar title/tooltips, `settings_screen.dart`'s title, the checklist list's "Before/After Mass" subtitle, and the Inventory/Notes "Add" button labels now all read through `AppLocalizations`, matching every key the ARB files already defined. Verified by re-checking every call site of the two changed function signatures (`rankLabel`/`seasonLabel` now take `BuildContext`).
- **What's still English-only, documented honestly rather than re-claimed as done**: the ~130 built-in checklist item labels, remaining dialog/screen copy (titles, hints, error messages), and — the bigger structural item — the celebration/feast *names* themselves, which live in the pure-Dart `liturgical_calendar` package as plain English strings and are outside Flutter's l10n system entirely. Translating ~45+ liturgical celebration names accurately is a content task for a bilingual reviewer familiar with liturgical terminology, not a mechanical string-table swap, and was deliberately not attempted here. See the updated doc comment in `checklist_detail_screen.dart` for the full, current, accurate status — replacing the round-1 comment that overstated it.
- Manual review only, as in every prior round — **run `dart test` / `flutter analyze` / `flutter run` yourself.**

**Round 6** (this pass): shifted focus to the pure-Dart `liturgical_calendar` package itself — the safety-critical core that round 1's Python cross-check only verified for Easter's *date*, not for season/rank/precedence logic. A full read of `computus.dart`, `season_engine.dart`, `calendar_engine.dart`, `general_roman_calendar.dart`, `cycles.dart`, and `models.dart` against the Roman Missal's General Norms for the Liturgical Year, n. 59 (Table of Liturgical Days).
- **Real content/precedence gap found and fixed**: Monday, Tuesday, and Wednesday of Holy Week had no explicit entries anywhere — they fell through to the generic Lenten-weekday filler, so a sacristan checking, say, Holy Tuesday would see "Lenten Weekday, Week 6" rather than anything naming Holy Week. Worse, per the Missal's General Norms n. 59, these three days sit in the very top precedence tier (I.2) alongside Ash Wednesday and the four great solemnities — nothing may be celebrated in their place, not even a solemnity — but the engine's precedence table (`_tier` in `calendar_engine.dart`) had no rule protecting them, so a parish adding a patronal solemnity via the local calendar editor that happened to land on one of these three days in a given year would have incorrectly displayed that solemnity as primary instead of "Monday/Tuesday/Wednesday of Holy Week." **Fixed**: added `mondayHolyWeek`/`tuesdayHolyWeek`/`wednesdayHolyWeek` to `MovableDates` (`computus.dart`), three new named movable celebrations in `general_roman_calendar.dart`, and extended the top-precedence key set in `_tier()` (renamed from `topSolemnityKeys` to `topPrecedenceKeys` for accuracy, since Ash Wednesday and these three days are ferial in *rank* but top-tier in *precedence*) to include them. Added regression tests (`calendar_engine_test.dart`) covering both the naming and the local-calendar-can't-override behavior; independently re-verified the three new day-of-week offsets in Python across five years (all Monday/Tuesday/Wednesday as expected, cross-checked against the same computus formula).
- **Documented, not changed, a subtler structural note**: `CalendarSource.computed` is shared by both movable named celebrations (Ash Wednesday, Easter, Trinity Sunday, etc.) and the generic per-day filler — meaning the source-based tie-break in `resolveLiturgicalDay` doesn't actually distinguish a movable celebration from filler by source (precedence tiering handles that correctly instead). This only matters in the rare case of two same-tier candidates from different sources on one day; no reproducible bug was found from it, so a source-enum split was deliberately not attempted without a compiler to verify the refactor — see the doc comment now on `CalendarSource` in `models.dart`.
- Everything else in the package (`computeEasterSunday`, `adventFirstSunday`, `baptismOfTheLord`, `holyFamily`, `christTheKing`, `weekStartingSunday`, the Ordinary Time part-1/part-2 week-numbering algorithms, the Sunday/weekday lectionary cycle logic) was traced by hand against the real liturgical rules and found correct, including the backward-from-34 Ordinary Time Part 2 numbering technique, which is the right approach and matches the round-1 60-year invariant test already in place.
- Manual review only — **run `dart test` yourself**; the three new day fixtures above are new since round 1's Python cross-check and have not been executed by any test runner.

**Round 7** (this pass): a dedicated accessibility pass — the original spec calls for accessibility compliance, and no round so far had checked it as its own focused item (only individual widgets like `BigCheckboxTile` had accessibility considerations baked in from round 1).
- **Icon-only buttons missing accessibility labels, found and fixed**: a repo-wide sweep for every `IconButton` found 5 of 17 with no `tooltip` set — on `IconButton`, Flutter uses `tooltip` as both the visible long-press hint *and* the screen-reader semantic label, so a missing one means a screen-reader user hears nothing useful for that control. Fixed: the delete buttons on Profiles, Contacts, and Suppliers now say "Delete {name}" (naming the specific item, not just "Delete"), and the inventory quantity +/- steppers now say "Decrease/Increase quantity."
- **Contrast checked, not just assumed**: every `swatchFor()` background/foreground color pairing in `theme/app_theme.dart` was run through the actual WCAG relative-luminance contrast formula in an independent Python script (not eyeballed) — all seven liturgical colors clear 6.5:1, comfortably above the 4.5:1 AA threshold for normal text. No fix needed here; documented as verified rather than left as an unchecked assumption.
- **`docs/STORE_CHECKLIST.md` gained an Accessibility section** listing what's verified (color+text pairing, contrast, touch targets, icon-button labels) versus what still needs a real device pass (an actual screen reader, and system text-scaling/layout overflow) — neither of which this sandbox can perform without a Flutter runtime.
- Manual review + one independently-executed contrast calculation — same standing caveat as every round: **a real screen-reader pass on a physical device remains the step this sandbox cannot do for you.**

**Round 8** (this pass): a user actually ran this project's CI on GitHub Actions — the first real, executed compiler/test feedback this project has ever gotten. Both jobs came back green (`dart test` on the calendar engine, `flutter analyze` on the whole app). That's genuinely good news, but it also proved a point made throughout this document: static analysis passing is not the same as the app working, because this round found a bug analyze could never have caught.
- **Critical runtime bug found and fixed**: `main.dart`'s `MaterialApp` never included `AppLocalizations.delegate` in its `localizationsDelegates` list — only the three Flutter-framework delegates (Material/Widgets/Cupertino). Every `AppLocalizations.of(context)!` call added in round 5 (the nav bar, `rankLabel`/`seasonLabel`, several screen titles and buttons) uses a null-assertion on that call's result. Without the delegate registered, `Localizations.of<AppLocalizations>(context, AppLocalizations)` always returns null — meaning `AppShell.build()`, which calls this on its very first line, would have thrown "Null check operator used on a null value" and crashed the app immediately on launch, in every locale, every single time. This is a real, severe, previously-undiscovered defect that seven prior rounds of manual review — including the round-5 pass that did the localization wiring itself — missed, because it's a *runtime widget-registration* problem, not a type error, and nothing short of actually running the app (or a targeted widget test) surfaces it. Fixed by adding the import and the delegate to the list, with a comment explaining exactly why.
- **Real app-level tests added** (`app/test/`, previously empty — the CI `flutter test` step existed since round 6 but was `continue-on-error: true` for lack of anything to run): `l10n_delegate_test.dart` pumps a `MaterialApp` with the app's real delegate list and asserts `AppLocalizations.of(context)` actually resolves in all three shipped locales — a direct regression test for the bug above, so it cannot silently reappear. `theme/app_theme_contrast_test.dart` ports round 7's one-off external Python contrast check into a permanent `flutter test` against the real `swatchFor()` function, so every liturgical color pairing is re-verified on every CI run instead of relying on a script outside the repo. `features/common/big_checkbox_tile_test.dart` covers the tap-to-toggle interaction and checked-state semantics of the core checklist widget. The CI workflow's `flutter test` step no longer has `continue-on-error: true` — it now genuinely gates the build.
- This is the clearest evidence yet for the standing message in this document: **manual review has a ceiling, and running the actual app is not optional.** The next most valuable step is a real device/emulator smoke test (`flutter run`) — nothing shy of that would have caught this bug before a real sacristan did.

**Round 9** (this pass): the user pushed round 8's fixes to their GitHub repo and re-ran CI — this time both jobs actually failed, and unlike round 8's find, neither failure was something manual review had a chance of catching: both only exist because of specifics of the installed toolchain versions on the runner. This is the first round where every finding came purely from a real, executed build/test run rather than any reading of the code.
- **`app-analyze` job failed at `flutter pub get`** with a version-solving error: `app/pubspec.yaml` pinned `intl: ^0.19.0`, but the Flutter SDK version GitHub's runner installed ships a `flutter_localizations` that itself depends on `intl ^0.20.3` — an app can never pin a lower `intl` than what `flutter_localizations` (a framework package, not something this repo controls) demands. **Fixed**: bumped the pin to `intl: ^0.20.3` in `app/pubspec.yaml`, with a comment noting that if a future Flutter upgrade shifts this requirement again, `flutter pub get`'s own error message names the exact version to use — it's not a value to guess at. No app code imports `intl` directly (only the generated localization code and `flutter_localizations` itself use it), so this was a safe, contained fix.
- **`calendar-engine-tests` job failed at `dart analyze`** with 33 `prefer_const_constructors` issues, all in `packages/liturgical_calendar/lib/src/general_roman_calendar.dart`. Every entry in `generalRomanCalendarFixed` was written as `FixedCelebration(month, day, const Celebration(...))` — the inner `Celebration(...)` was already `const`, but the outer `FixedCelebration(...)` call (itself a `const` constructor, being fed only integer literals and a const `Celebration`) was not, so it could have been `const` and the linter said so. This is exactly the rule this package's own `analysis_options.yaml` explicitly opts into (`prefer_const_constructors: true`, not just whatever `package:lints/recommended.yaml` defaults to) — so the correct fix was the code, not loosening the CI's `--fatal-infos` flag to let the repo's own declared lint rule go unenforced. **Fixed**: all 33 call sites now read `const FixedCelebration(month, day, Celebration(...))` — `const` moved to the outer call, redundant inner `const` removed. Every non-`const` object allocation on every calendar lookup is now avoided, a small but real performance benefit alongside satisfying the linter.
- Both fixes were verified by direct inspection against the CI's own logged error messages (the exact `intl` version the resolver named, and the exact rule/line pattern the analyzer flagged) rather than guessed at — but, as always, **this sandbox still cannot run `flutter pub get` or `dart analyze` itself to confirm; the next CI run is the real confirmation.**
- Manual review only, verified line-for-line against the round-8 CI failure screenshots — **push these two fixes and re-run CI to get the real confirmation.**

**Round 9, continued** — the user pushed those two fixes and re-ran CI. Both jobs got further, then failed again on two *different* real problems — direct proof the round-9 fixes above were correct (they cleared the previously-reported errors) and that this is exactly the iterative "push, fail on the next real thing, fix, push again" loop CI is supposed to drive.
- **`calendar-engine-tests` failed at `dart analyze`** with 1 issue: `warning - lib/src/calendar_engine.dart:1:8 - Unused import: 'computus.dart'. - unused_import`. This had actually been present all along — the round-9 screenshot showing "34 issues found" was 33 `prefer_const_constructors` (fixed) plus this 1 `unused_import`, which just wasn't visible in the tail of that screenshot. Confirmed by grep that no symbol from `computus.dart` (`computeEasterSunday`, `MovableDates`, etc.) is referenced anywhere in `calendar_engine.dart` — the import was dead. **Fixed**: removed the import line.
- **`app-analyze` failed at `flutter pub get`** with a new version-solving conflict (the `intl` conflict from earlier in round 9 is confirmed resolved — the job got past it to a different failure): `share_plus >=8.0.1 <10.0.1` depends on `web ^0.5.0`, but `drift >=2.19.2` (already required) depends on `web ^1.0.0` — incompatible, and `app/pubspec.yaml` pinned `share_plus: ^9.0.0`. pub's own resolver suggested the fix directly in its output (`flutter pub add share_plus:^13.3.0`). **Fixed**: bumped the pin to `^13.3.0`, matching the resolver's suggestion exactly rather than guessing a version. Checked whether this breaks the one call site that uses the package (`inventory_screen.dart`'s CSV-export share sheet, `Share.shareXFiles(...)`): the static `Share` class was deprecated in share_plus 11.0.0 in favor of `SharePlus.instance.share(ShareParams(...))`, but per its own changelog remains present and functional through 13.x, so the call site was left as-is (now flagged only as an `info`-level deprecation, and `app-analyze`'s `flutter analyze` step doesn't use `--fatal-infos`, so this won't fail the build) rather than rewritten to an API this sandbox can't compile-check.
- Both fixes came from reading the CI logs' own error text precisely (an unused-import warning naming its own file/line; a resolver naming its own suggested fix) — the same pattern as every fix in this round. **Still not confirmed by a passing CI run** — that confirmation is the next thing to watch for.

**Round 9, third CI run** — both dependency-resolution problems are confirmed fixed: both jobs got past `pub get` for the first time and reached `dart test` / `flutter analyze` for real. Each then failed on something new.
- **`calendar-engine-tests` failed at `dart test`** — 3 tests failed, and this is a genuine logic bug in the precedence engine itself, not a toolchain/lint issue: `resolveLiturgicalDay`'s tie-break for same-tier candidates used `c.source == _defaultSource` to detect "this is just the generic filler, prefer the named entry instead." But `CalendarSource.computed` (`_defaultSource`) is *also* the source of every named movable celebration (Easter, Pentecost, Palm Sunday, Divine Mercy Sunday — see `movableCelebrationsForYear`), not just the filler — `models.dart`'s doc comment on `CalendarSource.computed` had already flagged this exact ambiguity as a known risk back in round 6, deferred for lack of a compiler to verify a fix. Round 9's first real `dart test` run finally exercised the code paths where it bites: Easter Sunday and the generic "1st Sunday of Easter" filler tie at precedence tier 1 (both top-tier, both Sunday, same source), and the tie fell back on list order — not guaranteed stable by `List.sort` — so the filler sometimes won, reporting `goldPermitted: false` instead of Easter's actual `true` (and similarly Pentecost's color as white instead of red). A related bug in the same function: the Sunday-precedence check (`if (isSunday) return 5;`) tested whether the *date* was a Sunday, not whether the *candidate itself* was the Sunday-rank celebration — so a Diocesan-level local optional-memorial entry landing on an actual Sunday tied with, and (via the same source-based bug) could beat, the real Sunday. **Fixed**: added `_isGenericFiller(Celebration c) => c.key.startsWith('default')` — every filler celebration's key is built with a `'default'`/`'default.privileged'` prefix and no named celebration anywhere in the package uses that prefix, so this unambiguously identifies the filler regardless of shared `source`. Replaced every `c.source == _defaultSource` filler-detection check (the tie-break, the tier-7 "plain ferial default" check, the post-sort filter) with `_isGenericFiller(c)`, and fixed the Sunday tier check to `isSunday && c.rank == CelebrationRank.sunday`. Updated `models.dart`'s `CalendarSource` doc comment, which had described the old (buggy) behavior as by-design, to describe the actual fix. This is the first bug this project has found via a genuinely *failing* assertion in an existing test (round 8's bug was found by writing a *new* test; every fix before that was manual review) — real proof the test suite is now doing its job.
- **`app-analyze` failed at `flutter analyze`** with 42 issues (`exit code 1`) — the app's own code is being analyzed by a real `flutter analyze` for the first time ever in this project's life (every prior run failed before reaching this step). Confirmed and fixed from the visible tail of the log: `app/test/l10n_delegate_test.dart` referenced `GlobalMaterialLocalizations`/`GlobalWidgetsLocalizations`/`GlobalCupertinoLocalizations` without importing `package:flutter_localizations/flutter_localizations.dart` (those three delegate classes live in that package, not in `package:flutter/material.dart`) — 4 of the 42 issues traced to this one missing import. Fixed by adding it. Also migrated `inventory_screen.dart`'s CSV-export share call from the deprecated `Share.shareXFiles(...)` (deprecated since share_plus 11.0.0) to the current `SharePlus.instance.share(ShareParams(...))` API, since a `deprecated_member_use` notice for that exact call is a very likely contributor to the 42 and the current API is a small, low-risk swap with a confirmed-correct signature. **The screenshots only showed the log's tail — the other ~38 of 42 issues are not yet visible to me and have not been fixed.** Next step: get the rest of that step's output (scroll up in the raw log, use the "Search logs" box for `error •` / `warning •`, or download the full log via the run's "..." menu) so the remaining issues can be diagnosed from their actual text rather than guessed at.

**Round 9, fourth CI run** — the user re-pushed and searched the log for "error", surfacing three real `error`-level issues (as opposed to the earlier `info`-level `prefer_const_constructors` items). `liturgical_calendar package — dart test` is now fully **green** — confirms the round-9-third-run precedence-engine fix was correct. `app-analyze` still failed at `flutter analyze`, now on genuine type/compile errors rather than just lints:
  - **`lib/data/repositories.dart:104` and `:131`**: `m.massType.equals(type)` / `t.massType.equals(type)` — `argument_type_not_assignable`, "The argument type 'MassType' can't be assigned to the parameter type 'String'." `massType` is declared `textEnum<MassType>()()` in `database.dart` — a drift type-converted column. Looked this up against drift's own API docs (`GeneratedColumnWithTypeConverter`): `.equals(S compare)` expects the raw *stored* SQL type (`String`), while `.equalsValue(D? dartValue)` is the method that compares against the *converted* Dart type (`MassType`) and is exactly what these call sites need. **This means round 3's finding — "`.equalsValue()` isn't a real drift method" — was itself wrong**: `equalsValue` is real and documented, and it's specifically for this situation. Whatever round 3 "fixed" by replacing it with `.equals()` introduced this exact bug; round 9's CI is what finally caught it. Fixed both call sites (`.equals(type)` → `.equalsValue(type)`), and grepped every other `textEnum`-column comparison in the app (`role`, `category`) to check for the same mistake elsewhere — found and fixed one more, `admin_pin_screen.dart:35`'s `p.role.equals(ProfileRole.admin)` → `.equalsValue(...)`, which the visible log hadn't reached yet but shares the identical root cause.
  - **`lib/features/calendar_detail/calendar_detail_screen.dart:110`**: `undefined_method` — `swatchFor` isn't defined for `_CalendarDetailScreenState`. The file calls the top-level `swatchFor()` function from `theme/app_theme.dart` without importing that file. Fixed by adding the import. Checked every other call site of `swatchFor()` in the app for the same missing-import mistake and found one more, `dashboard_screen.dart` (2 call sites) — fixed there too.
  - **`lib/features/checklists/builtin_templates.dart`**: the visible `info`-level issues (lines 52–56 in the log, `prefer_const_constructors`) are the same pattern already fixed once in `general_roman_calendar.dart` this round — every one of the file's 16 `BuiltinTemplate(...)` list entries had const `BuiltinItem`s inside a non-const list inside a non-const `BuiltinTemplate(...)` call. Fixed by making every entry (and its `items` list, where inline) fully `const`, dropping the now-redundant inner `const` keywords on each `BuiltinItem`.
  - As before, the log's full 42-issue count wasn't entirely visible — these were the ones the "Search logs" `error` filter and the log's visible tail surfaced. **Still likely more to find on the next run.**

**Round 9, fifth CI run** — down to 11 issues (from 42), all visible this time via the same "Search logs" `error` trick plus the log's tail, and only 1 of the 11 was error-level:
  - **`test/features/common/big_checkbox_tile_test.dart:43`**: two issues on one line, `semantics.hasFlag(SemanticsFlag.isChecked)`. `undefined_identifier` (the real error): `SemanticsFlag` lives in `dart:ui` and, in this Flutter SDK version, isn't surfaced transitively through `package:flutter/material.dart` — fixed with a direct `import 'dart:ui' show SemanticsFlag;`. `deprecated_member_use` (info): `hasFlag()` itself is deprecated since Flutter 3.32 in favor of `flagsCollection` (a `SemanticsFlags` object whose `isChecked` getter returns a tri-state `CheckedState`, not a plain bool — a real API shape change, not a rename). Rather than guess at `CheckedState`'s exact member names with no compiler to check the guess against, silenced just this one lint with `// ignore: deprecated_member_use` and left a comment explaining why — migrating to `flagsCollection` is deferred to a round where a real `flutter test` run can confirm the replacement.
  - **`lib/features/settings/language_screen.dart:27`** (`deprecated_member_use`): `RadioListTile`'s own `groupValue`/`onChanged` are deprecated — Flutter's Radio API redesign moved group ownership to an ancestor `RadioGroup<T>` widget (confirmed against Flutter's own migration guide, docs.flutter.dev/release/breaking-changes/radio-api-redesign). Migrated: `LanguageScreen` now wraps its `RadioListTile`s in a `RadioGroup<String>` carrying `groupValue`/`onChanged`, and each `RadioListTile` only sets `value`.
  - **`lib/features/settings/settings_screen.dart:84`** and **`lib/theme/app_theme.dart:50`** (`prefer_const_constructors`): the "Appearance" `ListTile` (no dynamic values, could be fully const) and `checkboxTheme: CheckboxThemeData(...)` (const-eligible args). Both made `const`, dropping the now-redundant inner `const` on `app_theme.dart`'s `VisualDensity`.
  - This run's fixes bring the known-issue count from 42 down to (at most) 0 pending confirmation — but "0 visible in the screenshot" has been wrong before this round (run 3 showed a partial 4-of-42 tail that turned out to have ~38 more hiding above it). **Not confirmed green yet.**

**Round 9, sixth CI run** — down to 5 issues, all fully visible in one screenshot this time (the log's own line count matched what was shown, unlike earlier runs). All 5 were the same rule, `use_build_context_synchronously`: a `BuildContext` used after an `await` with no `mounted` check guarding it in between — a real correctness concern (the widget could be unmounted by the time control returns, and using its context then can throw or silently no-op).
  - **`inventory_screen.dart`** (4 of the 5): `_textLowStockAlert` used `context` right after `await repo.watchLowStock().first` (no guard), and again after a second `await ...watchAll().first` (the file's *existing* `if (!context.mounted) return;` was placed one `await` too late, only covering the third usage). `_exportLowStockCsv` had the identical first-usage gap. Fixed by adding a `context.mounted` check immediately after each `await`, consolidating the redundant later check into the earlier one where possible.
  - **`admin_pin_screen.dart`** (1 of the 5): inside an IIFE (`(() async {...})()`), `context.read<SacristanDatabase>()` was called *after* `await profileRepo.addProfile(...)`. Rather than add a guard mid-closure (awkward given the closure returns a value used via `??=`), hoisted the `context.read` call to *before* the `await` — it doesn't depend on the awaited result, so this fully removes the async gap instead of just checking around it.
  - Checked every other `Future<void> _show...Dialog(BuildContext context, ...)` helper in the app for the same pattern (`profiles_screen.dart`, `contacts_suppliers_screen.dart` ×2, `local_calendar_editor_screen.dart`, `reminders_screen.dart`) — `flutter analyze` didn't flag any of them, and unlike earlier runs this run's issue count matched what was actually shown, so there's good reason to trust that's a complete, not partial, picture for this specific rule.
  - **Confirmed green.** The user reported the run passed — both `liturgical_calendar package — dart test` and `app-analyze` (pub get, codegen, `flutter analyze`, `flutter test`) are fully clean for the first time in this project's life. This closes out round 9's CI-iteration thread: six real runs, six rounds of real fixes, going from "nothing has ever been compiled" to a fully green pipeline with real tests and real lint rules both enforced, no `continue-on-error`, no `--fatal-infos` loosened. Everything fixed along the way is listed above in this section, in the order the compiler actually found it — treat that ordering as a rough map of what a from-scratch manual review missed, worst first: a precedence-engine logic bug (round 9 run 3), a backward-since-round-3 drift API misuse (run 4), two missing imports (run 4), a Flutter framework breaking change (run 5), and five `BuildContext`-after-`await` bugs (run 6). None of these were found by any of rounds 1-8's manual review, including the line-by-line pass in round 4 — the honest lesson standing at the end of round 9 is the same one repeated throughout this document, now with six fresh data points behind it: **manual review has a real ceiling, and only running the actual toolchain finds what's past it.**
  - **What CI passing does *not* mean**: `flutter analyze` and `dart test`/`flutter test` never launch the app on a device or simulator — they check types, lints, and the specific assertions this project's own tests happen to make. A clean CI run is necessary but not sufficient for "this app works." The next-highest-value step, unchanged from every earlier round's closing note, is a real `flutter run` on an actual device or emulator — the one thing this sandbox has never been able to do and CI doesn't cover either.

**Round 10** (this pass): with CI now green, the highest-value manual review left is exactly what round 9 just proved CI *can't* reach — cross-session and runtime-probabilistic correctness, neither of which a type checker or a single `flutter test` process can exercise. Two real bugs found by that lens, both in code that predates round 9 and that CI's now-green state gave no signal about either way:
- **`database.dart`'s `newId()` (every table's primary-key generator) had no real uniqueness guarantee.** It combined `DateTime.now().microsecondsSinceEpoch` with `identityHashCode(Object())` — but `DateTime.now()`'s resolution isn't guaranteed sub-microsecond on every platform this app ships to (iOS/Android/Windows), and `identityHashCode()` is explicitly not documented as collision-free (two distinct objects can share one). The first place this runs at any volume is `main.dart`'s first-launch seeding loop — roughly 100 sequential `await ...insert(...)` calls building the built-in checklist templates, before the UI even shows. A collision there is a primary-key violation that throws on insert: the app would crash on its very first launch, for reasons no CI run would catch (a collision is a runtime probability, not a type error). **Fixed**: `newId()` now draws 128 bits from `Random.secure()` (a CSPRNG, part of `dart:math` — no new package, avoiding any repeat of round 9's dependency-conflict saga) and formats it as a standard-looking v4 UUID. Added `app/test/data/database_id_test.dart`: generates 5000 ids in the same tight-loop shape as the seeding loop and asserts no duplicates, plus a format sanity check.
- **`notifications_service.dart` derived the int id `flutter_local_notifications` requires from `id.hashCode & 0x7fffffff`.** Dart's own docs on `Object.hashCode` state values "need not be consistent between executions of the same program" — but `scheduleReminder()` and `cancel()` are routinely called from *different app sessions* (add a reminder today, cancel it next week after restarting the app), and the underlying OS-level notification persists independent of whether the app process that scheduled it is still running. If a later session's `.hashCode` for the same reminder-id string ever differs from the session that originally scheduled it, `cancel()` derives the wrong notification id: the database row correctly shows the reminder as canceled, but the real OS-level notification was never told to stop and fires anyway. This is a genuine, if execution-dependent, correctness bug that neither this sandbox nor a single-process `flutter test` run could ever reproduce (it requires two separate process runs to manifest) — exactly the kind of thing that needs to be reasoned about rather than observed. **Fixed**: replaced `.hashCode` with a hand-rolled FNV-1a hash (`stableNotificationId()`) — an algorithm this file owns outright, so it can never drift between what schedules a notification and what later cancels it, regardless of any future Dart SDK change to `hashCode`'s implementation. Added `app/test/data/notifications_service_test.dart` covering determinism, valid output range, and a basic distinctness sanity check.
- **Found, not fixed — a documented but unimplemented feature, not a bug**: the `Reminders` table has a `repeatRule` column (`null | 'weekly' | 'yearly'`) and `ReminderRepository.add()` accepts it, but nothing wires it up — `reminders_screen.dart`'s add-reminder dialog never collects a repeat choice from the user (always passes nothing, so it's always `null` in practice), and `NotificationsService.scheduleReminder()` has no repeat parameter and never passes `matchDateTimeComponents` to `zonedSchedule()` for real OS-level repeating. This is scoped like the existing Windows-toast gap: real, worth knowing about, and more of a feature addition (repeat-choice UI + repeat-aware scheduling) than a bug fix — left for a future round rather than attempted half-finished here.
- Manual review only, reasoned against Dart/Flutter's own documented API contracts (`Object.hashCode`'s stability disclaimer, `identityHashCode`'s non-uniqueness) rather than observed failures — this sandbox still can't run the app to watch either bug reproduce. **Still true, and now with two more concrete examples of why: a real device/emulator smoke test remains the one verification this project has never had.**
- **Confirmed green.** The user pushed these fixes and re-ran CI; it passed. Both new test files (`database_id_test.dart`, `notifications_service_test.dart`) ran for real and confirmed the fixes work as reasoned, not just as written. This closes out round 10's coding thread — the two bugs above are real, fixed, and now covered by regression tests, on top of round 9's already-green analyze/test baseline. The next milestone isn't another CI run — it's the device/emulator smoke test flagged at the end of round 9 and repeated above: the one form of verification this project has never had, and the only remaining source of unknown-unknowns at this point.

**Round 11** (this pass): with CI green through round 10, continued the same cross-session/runtime lens on the areas round 10 flagged as not-yet-reviewed, plus one specific loose thread round 10 left open (`notifications_service.dart`'s timezone handling).
- **Investigated and resolved: `notifications_service.dart`'s hardcoded `tz.setLocalLocation(tz.getLocation('UTC'))` is not a bug, and its old comment was actively misleading.** The comment claimed this "falls back to UTC if the platform's local zone can't be read" — but nothing in the code ever attempts to read the real device zone; it's an unconditional hardcode, not a fallback. The real question was whether this causes reminders to fire at the wrong wall-clock time for a non-UTC user. Fetched the `timezone` package's own source for `TZDateTime.from(DateTime other, Location location)` (the constructor `scheduleReminder()` uses) to check rather than assume: its implementation is `this._(_toNative(other).toUtc(), location, ...)` — the resulting absolute instant comes from calling `.toUtc()` on the *input* `DateTime` directly, using Dart's own (OS-backed) notion of the device's real local zone. The `location` argument passed in (`tz.local`, hardcoded to UTC) only affects the *display* zone label/offset carried by the returned `TZDateTime` — not the instant used to actually schedule the notification. Confirmed the app only ever calls this with plain `DateTime(year, month, day, hour, minute)` values built directly from `showDatePicker`/`showTimePicker` results (`reminders_screen.dart`) — i.e. real device-local wall-clock values already — and confirmed (by grep) that `tz.local`/`TZDateTime` are used nowhere else in the app, including nowhere that reads back a scheduled time's `.hour`/`.minute`/zone name for display (the Reminders list shows the plain `DateTime` stored in the database, not anything derived through `tz.local`). Conclusion: reminders fire at the correct real-world time regardless of this hardcoding. **Fixed the comment** (it now explains the actual, verified reasoning above instead of describing a fallback attempt that was never there) rather than changing any behavior — this is a case where the honest finding is "not a bug," and the value added is closing the investigation with a citation-backed answer instead of leaving a misleading comment for a future round to re-open.
- **Found, not fixed — a second documented-by-omission gap, same shape as the `repeatRule` gap round 10 flagged**: `InventoryItems.lowStockThreshold` (a nullable `IntColumn`) exists in the schema but is entirely dead — no UI field sets it (the add/edit item dialog in `inventory_screen.dart` has no threshold input), `InventoryRepository` never reads or compares it, and `lowStockFlag` is set purely by the sacristan manually long-pressing an item. The column's presence implies an intended "auto-flag when quantity drops to/below a threshold" feature that was never wired up. Deliberately not implemented this round: doing so well means a real UX decision (does an auto-threshold override the manual toggle, or only set it and never auto-clear it?) that risks guessing at intent no one has asked for, the same reasoning round 9 applied when it declined to guess at `CheckedState`'s replacement API shape. Documented here and in README so a future round (or the user) can decide the intended behavior before it's built.
- **Reviewed and confirmed correct, no changes needed**: the CSV export in `inventory_screen.dart`'s `_exportLowStockCsv` uses `package:csv`'s `ListToCsvConverter().convert(rows)` rather than hand-built string joining — that package handles comma/quote/newline escaping per the CSV spec, so item names or notes containing commas or quotes (e.g. `notes: 'needs re-gilding, chip on rim'`) can't corrupt the exported file's column structure. The dashboard's "today" resolution (`dashboard_screen.dart`'s `_load()`) uses `DateTime.now()` truncated to midnight via the plain `DateTime(now.year, now.month, now.day)` constructor — this is the device's real local calendar day, which is exactly what a sacristan physically standing in their own parish wants; no timezone bug here (this is a different code path from the reminders timezone question above, and doesn't touch `package:timezone` at all). Both of round 10's flagged-as-unreviewed seeding functions (`seedBuiltinTemplatesIfNeeded`, `seedReferenceEntriesIfNeeded`) were re-checked: both are idempotent by design, both already carry an accurate comment describing their one accepted edge case (a parish that deletes every seeded row of one kind can trigger a harmless re-seed), and neither has a bug beyond what's already documented.
- **Found and fixed a real, user-reachable bug class: no dialog or list-tap in the app guarded against a double-tap re-running its `await`-based create action before the first tap finished.** Every "Add" flow (and the checklist-template list's tap-to-open flow) does at least one real `await` — a DB insert, sometimes two — before popping its dialog or navigating away, and nothing disabled the triggering button/tile in between. A sacristan double-tapping (an easy, common thing to do — impatience, an unresponsive-feeling UI, or literally just a fast double-tap gesture) could run the handler twice, and since every affected call generates a fresh row id (`Value.absent()`/`clientDefault`), the second run doesn't update the first — it silently creates a second row. The worst instance: `checklist_list_screen.dart`'s `_openTemplate` (opening a checklist template from the list) does up to three sequential awaits — including, for a single-Mass parish's common case, auto-creating *today's Mass* — before navigating to the checklist; a double-tap there could create two separate `Mass` rows for the same day/type, each then getting its own independently-progressing checklist instance, directly contradicting that file's own doc comment claiming progress is "never silently duplicated." Also real, if lower-stakes: `admin_pin_screen.dart`'s "Set PIN and unlock" button (the very first screen a fresh install's admin sees — exactly when an eager double-tap is likely) could create two separate "Parish Admin" profiles, each with its own PIN. **Fixed in all nine affected files** (`checklist_list_screen.dart`, `admin_pin_screen.dart`, `inventory_screen.dart`, `reminders_screen.dart`, `contacts_suppliers_screen.dart` ×2, `profiles_screen.dart`, `local_calendar_editor_screen.dart`, `reference_screen.dart`, `notes_screen.dart`) with the same pattern: a local `submitting`/`_opening` boolean, set before the first `await` and checked to disable the button/tile until the flow finishes (`finally`-guarded so an exception doesn't leave the control permanently disabled). Two plain `AlertDialog`s with no prior local state (`contacts_suppliers_screen.dart`'s supplier dialog, `reference_screen.dart`'s entry dialog) needed wrapping in `StatefulBuilder` to get a `setState` for this. Reviewed every other `onPressed: () async` site in the app (via grep) to confirm these were the only ones performing an unguarded fresh-row insert — pure toggle/delete/cancel actions elsewhere are naturally idempotent under a repeat call and were left as-is.
- **Also closed the same class of bug at its root, not just at this one screen: `ChecklistRepository.findOrCreateInstance` had a genuine check-then-insert race with no lock of its own**, independent of any particular caller's UI. Added an in-memory `Map<String, Future<String>>` keyed by `"$massId:$templateId"` so any two concurrent calls for the same pair — from this screen's now-fixed UI, or any future caller — share one in-flight lookup-or-create instead of each independently missing the other's not-yet-committed insert. This is the first place in the project where a repository method itself needed concurrency-safety, not just its caller. Added `app/test/data/checklist_instance_race_test.dart`, which reproduces the exact race in-process (two calls started via `Future.wait` without awaiting the first individually — Dart's single-threaded event loop still interleaves them at their `await` points) and asserts both resolve to the same instance and only one row is created; a second test covers different (Mass, template) pairs never incorrectly sharing a lock. This required adding `SacristanDatabase.forTesting(super.executor)` — a test-only constructor that opens against an in-memory `NativeDatabase` instead of the real on-device file (which needs a platform channel `flutter test` can't provide) — the first test in this project to exercise a real (if temporary) drift database rather than only pure functions. Before adding it, checked whether this risks reopening round 9's CI saga: fetched GitHub's own `actions/runner-images` software manifest for the `ubuntu-latest` runner this project's CI uses, and confirmed it ships `libsqlite3-dev`/`sqlite3` pre-installed — so `NativeDatabase.memory()` should resolve its native library without any extra CI setup, rather than assuming that from training knowledge alone.
- **Found and fixed the same class of race in the app's single most-used interaction: `ChecklistRepository.setTick`, called on every checkbox tap in the Checklist screen.** Same check-then-insert-or-update shape as `findOrCreateInstance` had, but reachable far more easily: `BigCheckboxTile` (`features/common/big_checkbox_tile.dart`) has no debounce of its own (`InkWell.onTap: () => onChanged(!checked)`), and its `checked` prop only flips once a write round-trips back through `watchTicks`'s stream — so two fast taps on the same row (an ordinary way to work through a checklist quickly) both fire with the same target value *before either write lands*. Both could see no existing tick row and both insert one; since drift's `getSingleOrNull()` throws when more than one row matches, every later tap on that item would then throw instead of toggling — silently bricking that one checkbox until someone edited the database directly. **Fixed differently from `findOrCreateInstance`'s lock**, deliberately: two concurrent `setTick` calls can carry genuinely different intended values (a fast tap-then-untap), so sharing one in-flight result (as `findOrCreateInstance` does, where every caller wants the same answer) would silently drop the second call's intent. Instead, each call is chained onto the previous call for the same `"$instanceId:$itemId"` key, so same-item calls always run one at a time, in the order they were made, rather than racing — every tap still applies, just serialized. Added `app/test/data/checklist_tick_race_test.dart`: one test reproduces two same-value concurrent taps and asserts exactly one row results; a second issues two *different*-value concurrent calls and asserts the second call in program order is what's ultimately stored (proving order is preserved, not just that duplication is prevented); a third covers the ordinary sequential case still working. Also grepped every other `getSingleOrNull()` call site in `repositories.dart` to check for the same shape elsewhere: `SettingsRepository.setValue` and `InventoryRepository.upsert` both use drift's atomic `insertOnConflictUpdate` (race-safe at the SQLite level, since both tables' conflict target is a real primary key) rather than an application-level check-then-write, and `verifyPin`/`getValue` are pure reads with no follow-up write — no further instances of this bug class found.
- **Verified, not a bug: `packages/liturgical_calendar/lib/src/cycles.dart`'s lectionary cycle logic.** Checked `sundayCycleFor` (three-year A/B/C, switching at Advent, formula `(N+1) % 3`) against several recent real published cycle years and `weekdayCycleFor` (two-year I/II, switching at January 1st — deliberately *not* Advent, per its own doc comment) against the USCCB's own FAQ page, which states the Sunday cycle explicitly does not change on January 1 (it changes at Advent) while describing the weekday cycle purely in terms of odd/even *calendar* years with no such exception — confirming the code's claimed Jan-1 switch point rather than assuming it.
- **Found and fixed a real, if minor, UX gap: `openSmsComposer` (the SMS deep-link helper used by the contacts/suppliers "text" buttons and the inventory low-stock alert) discarded its own success/failure result at every call site.** On this app's Windows build — one of its three shipped targets — there is no registered handler for the `sms:` URI scheme at all, so tapping "Text this contact" there does nothing visible: no crash, no message, just a button that appears broken. The same silent failure can happen on any device with no default messaging app configured. Added `openSmsComposerWithFeedback` (`features/common/sms_helper.dart`), which shows a SnackBar when the OS reports it couldn't open anything, and switched all three call sites (`contacts_suppliers_screen.dart` ×2, `inventory_screen.dart`) to it. This introduced a new `context` use after an `await` in `inventory_screen.dart`'s `_textLowStockAlert` (previously `openSmsComposer` didn't need `context` at all) — added the matching `context.mounted` guard rather than reintroducing a round-9-class bug while fixing something else.
- **Found and fixed a real, actively-misleading gap, not just an honest incompleteness: "Settings > Manage Checklist Templates" — named verbatim in two places already shipping in the app — did not exist.** `checklist_detail_screen.dart`'s empty-state message ("Add some from the template editor (Settings > Manage Checklist Templates)") and `settings_screen.dart`'s "Admin PIN" subtitle ("Protects template/inventory editing") both refer to a screen that was never built; the only way any template or item ever entered the database was the one-time built-in seed in `main.dart`. Unlike the `repeatRule`/`lowStockThreshold` dead-schema gaps documented above (schema present, no UI, left as a documented decision because the right behavior is a genuine product judgment call), this gap was actively misleading users toward a feature that flatly didn't exist — so it was built, not just documented. Added to `repositories.dart`: `watchAllTemplates()` (all templates, unfiltered by Mass type, ordered by name — distinct from the existing `watchTemplatesFor(MassType)` used by the "start a checklist" flow), `addTemplate({name, massType, phase})`, `addItem({templateId, label, latinTerm})`, `deleteItem(templateId, itemId)`, and `reorderItem(templateId, itemId, direction)`. Deliberately did **not** add a `deleteTemplate`: `ChecklistItems.templateId` cascades on template delete (see `database.dart`), but `ChecklistInstances.templateId` does not — a template any Mass has ever used has real historical checklist rows referencing it, and SQLite's default foreign-key enforcement would reject the delete outright rather than silently orphaning that history. Whether to block the delete with an explanation, cascade and accept the loss of history, or soft-hide the template instead is a product decision, not a bug fix — left for a future round, the same judgment call round 9 made for the `CheckedState` migration and round 11 already made once this round for `lowStockThreshold`. Recognized, while first drafting `addItem`/`deleteItem`/`reorderItem`, that they have the exact same check-then-write shape already found and fixed twice this round (`findOrCreateInstance`, `setTick`) — an admin tapping "add item" or a reorder arrow rapidly is a realistic way to trigger two concurrent calls for the same template — so all three are serialized per template through a new `_serializedForTemplate<T>(templateId, action)` helper (the same chain-onto-the-previous-call lock pattern `setTick` uses, keyed by `templateId`), and `reorderItem` re-queries the template's current item list itself each time it actually runs rather than trusting a caller-cached list (an earlier draft took a caller-supplied `List<ChecklistItem>`, which was corrected specifically because it would have reintroduced the same race one call site up). `addItem` computes its new item's `sortOrder` as one past the current *maximum*, not `existing.length`, so it can't collide with a sortOrder left behind by a deleted item. Built the UI on top: `ChecklistTemplateEditorScreen` (lists all templates via `watchAllTemplates()`, admin-gated "Add template" dialog with Mass-type/phase dropdowns) and `TemplateItemsEditorScreen` (per-template item list with admin-gated add/delete/reorder-up/reorder-down actions), both in the new `features/settings/checklist_template_editor_screen.dart`, gated behind `AdminSession.isUnlocked` the same way the Reference Library already gates its own add/delete — matching Settings' pre-existing claim that the PIN "Protects template/inventory editing." Wired a new "Manage Checklist Templates" `ListTile` into `settings_screen.dart` under a new "Checklists" section. Added `app/test/data/checklist_template_edit_race_test.dart`: covers `addTemplate` storing its fields correctly, two concurrent `addItem` calls both landing with distinct non-colliding `sortOrder` values, `deleteItem` removing only its target, `reorderItem` swapping adjacent items and no-oping past either end of the list, and — the direct regression case — several concurrent `reorderItem` calls on the same template still leaving every item with a distinct `sortOrder` afterward instead of a collided or lost one.
- **Found and fixed a real precedence-engine bug in the pure-Dart calendar package, the same class of bug round 9's CI runs already caught and fixed twice — but this occurrence had survived every round since, undetected by any existing test.** `calendar_engine.dart`'s `_tier()` function grants top precedence (tier 1) to "Sundays of Advent, Lent, and Easter" per the Roman Missal's Table of Liturgical Days (General Norms n. 59, I.2) — correct in principle, but the check that implemented it, `if (isSunday && (season == advent || season == lent || season == easter)) return 1;`, tested only the *date*, not which *candidate* `c` actually was. Since every candidate present on a given date is scored by the same `isSunday`/`season` parameters, this granted tier 1 to *every* candidate on such a date, not just the actual Sunday-rank one — including a fixed-date General Roman Calendar solemnity that happens to land on that Sunday. Concretely: the Immaculate Conception (Dec 8, a fixed solemnity) falls on a Sunday in 2024 and 2030 — verified with a plain day-of-week check, not assumed — and in both years it's specifically the Second Sunday of Advent. With the bug, both the Sunday (filler, tier 1 via the broken check) and the Immaculate Conception (solemnity, also tier 1 via the same broken check) tied, and the tie-break rule (any non-filler candidate sorts ahead of the generic filler) then incorrectly made the *solemnity* win as `.primary` — exactly backwards from the Missal's own precedence table, where Sundays of Advent/Lent/Easter (I.2) outrank General Calendar solemnities (II.4), and backwards from actual Church practice, which transfers such a solemnity rather than letting it displace the Sunday. This is the identical shape of bug round 9 found and fixed for tier 5 (`isSunday && c.rank == CelebrationRank.sunday` was needed there too, not `isSunday` alone) — it had simply never been caught at tier 1 because no existing test exercised a fixed-date solemnity coinciding with one of these specific Sundays; six rounds of CI runs and every prior manual review missed it for the same reason. **Fixed** by adding the missing `c.rank == CelebrationRank.sunday` guard to the tier-1 check, so only the day's actual Sunday-rank candidate gets bumped to tier 1 by virtue of the date — a coinciding solemnity now correctly falls through to `c.rank == CelebrationRank.solemnity → return 2`, letting the Sunday win as intended while the solemnity still appears as a secondary celebration in the day's list (not dropped — a parish still needs to know it's there). Verified the fix doesn't regress any of this file's other Sunday-vs-solemnity cases (Pentecost, Trinity Sunday, Corpus Christi, Christ the King, Palm Sunday) by tracing each one's rank/key/season through the corrected logic by hand, since this sandbox has no `dart` executable to run the suite directly. Added a new regression test to `packages/liturgical_calendar/test/calendar_engine_test.dart` covering exactly this scenario for both 2024 and 2030, asserting the Second Sunday of Advent wins as `.primary` and the Immaculate Conception still appears among the day's `celebrations`.
- **Found, not fixed — a third documented-by-omission gap, same shape as the `repeatRule` and `lowStockThreshold` gaps already noted this round: `ChecklistTicks.doneByProfileId` is dead end to end.** Grepped every `setTick(...)` call site in the app: there is exactly one (`checklist_detail_screen.dart`'s checkbox `onChanged`), and it always passes `null` for `profileId`. The schema column, the repository parameter, and the storage write all exist and work — nothing in the UI ever supplies a real value, because there is no concept anywhere of "which sacristan is currently checking things off." Pulling on that thread further: `ProfileRepository`/`Profile` (Settings > Sacristan Profiles) is used in exactly three files — `profiles_screen.dart` (list/add/delete), `admin_pin_screen.dart` (the one profile treated as "the" admin, for PIN purposes only), and `main.dart` (Provider wiring) — a volunteer-role profile, once created, is never read, selected, or referenced by anything else in the app. There is also no guard against creating more than one `role: admin` profile via the ungated "Sacristan Profiles" screen; `admin_pin_screen.dart`'s `_load()` always takes the first admin-role profile found (no `orderBy`, so "first" is whatever SQLite's default row order happens to be) as *the* admin identity for PIN purposes, which works but means a second "Admin"-labeled profile someone adds there is silently inert rather than erroring or being prevented. None of this is a crash or data-loss bug — it's an honest incompleteness, the same category as round 10's `repeatRule` gap: the data model anticipated per-sacristan attribution (who checked off "light the sanctuary lamp") and a real notion of "who's using the app right now," but no UI was ever built to capture or use either. Whether to build an active-profile switcher (and require or offer choosing one before ticking items), or to simplify the data model instead (drop `doneByProfileId` and the volunteer-role distinction if per-person attribution was never actually wanted), is a product decision — left undecided here rather than guessed at, the same judgment call made for `repeatRule`, `lowStockThreshold`, and template deletion.
- **Found and fixed a real, silent-data-loss-shaped bug in `local_calendar_editor_screen.dart`'s "Add local calendar entry" dialog: the Day dropdown always offered 1-31 regardless of the selected Month, and nothing anywhere validated the combination.** `CalendarRepository.addLocalEntry` inserts whatever `month`/`day` ints it's given with zero validation, and `LocalCalendarEntries` has no database-level constraint either — so an admin could pick, say, "February" and "31" (an easy, plausible mistake, not an edge case someone would need to go out of their way to hit) and the entry would save without any error. The calendar engine matches local entries with a plain `entry.month == date.month && entry.day == date.day` check (`calendar_engine.dart`) — since no real date is ever `(month: 2, day: 31)`, that entry would then never appear on the Dashboard or Calendar, on any date, ever, silently contradicting this screen's own promise that an added entry "will show up ... automatically, every year." There was no error, no warning, and no way for the admin to discover the mistake short of noticing their patronal feast never shows up. **Fixed** by filtering the Day dropdown's item list to the selected Month's real length (a `_daysInMonth` lookup table, Feb given 29 rather than 28 so a deliberate leap-day entry is still selectable — the engine already handles that correctly by simply not matching in non-leap years) and clamping the currently-selected day down whenever a month change makes it invalid (e.g. day 31 selected under January, then switching to February). Discovered, while implementing the clamp, that `DropdownButtonFormField`'s `initialValue` (the parameter round 9's Flutter-API-rename finding already renamed from `value`) is only consulted on that widget's first build, not on later rebuilds — so clamping the `day` state variable alone would not have visually updated the already-open Day dropdown; added a `ValueKey(month)` on the Day dropdown so Flutter treats it as a fresh widget whenever the month changes, correctly picking up both the clamped value and the filtered item list. Added `app/test/features/settings/local_calendar_editor_screen_test.dart` — this project's first widget test to exercise a real dialog against a real (if temporary) in-memory database rather than a pure repository call or an isolated leaf widget: it selects day 31 under January, switches to February, asserts the Day dropdown no longer shows or offers 31 and has clamped to 29, then submits and asserts the row actually saved to the database is `(month: 2, day: 29)` — a real date — rather than the pre-fix bug's silently-unreachable `(2, 31)`. On a second look at that test (still unrun by a real compiler), loosened two `findsOneWidget` assertions to `findsWidgets`: `DropdownButtonFormField`'s internals can keep more than one copy of the selected item's text in the tree for layout purposes even while closed, and the exact count isn't the point — the final `day: 29` database assertion is what actually proves the fix, independent of that count.
- **Independently re-verified the Sunday lectionary cycle formula** (already checked in rounds 9 and 11 against several published years and USCCB's FAQ) with a fresh live search rather than relying on a prior round's check: the 2025-2026 liturgical year (the one running through most of calendar year 2026) is independently published as Sunday Cycle A, Weekday Cycle II — matching `sundayCycleFor`/`weekdayCycleFor`'s output exactly. No change needed; recorded here as an added data point of confidence in already-verified code, not a new finding.
- Manual review only, as always — the next-highest-value step remains unchanged from rounds 9 and 10: a real `flutter run` on an actual device or emulator.

**Round 12** (this pass): the user, working directly from a fresh Windows machine with no prior Flutter install, was walked step by step through installing the Flutter SDK, Visual Studio (with the "Desktop development with C++" workload), enabling Windows Developer Mode (required for the symlink support Flutter's plugin build needs), generating the project's missing `windows/` platform folder (`flutter create --platforms=windows .` — this repo had never had one; every prior round's "manual review only, no compiler" caveat meant no one had ever actually tried a desktop build target before), and finally running `flutter run -d windows`. **This is the first time SACRISTAN has ever actually executed as a running app**, twelve rounds and one green CI pipeline in. It immediately crashed on launch, before a single screen rendered.
- **Found and fixed a real, 100%-reproducible startup crash: `AppTheme.light()`/`.dark()` (`theme/app_theme.dart`) built the app's `textTheme` as `Typography.material2021(platform: TargetPlatform.android).black.apply(fontSizeFactor: 1.05)`, which threw `'fontSize != null || (fontSizeFactor == 1.0 && fontSizeDelta == 0.0)': is not true.` on every launch — a "red screen of death" before `MaterialApp` ever painted anything.** Root-caused by fetching Flutter's own SDK source (`packages/flutter/lib/src/material/typography.dart`, `stable` branch) rather than guessing: `Typography.material2021(...).black` (and `.white`) is *deliberately color-only* — every one of its 15 `TextTheme` fields sets `color`/`fontFamily`/`decoration` but explicitly omits `fontSize`, by design (per that class's own doc comment); real font-size geometry lives in a separate `englishLike`/`dense`/`tall` theme that Flutter normally merges in itself, inside `ThemeData.localize()`, once a `MaterialApp` knows the active script category — i.e. *after* `ThemeData` construction, not before. `TextStyle.apply()` asserts `fontSize != null` whenever a non-1.0 `fontSizeFactor` is given, so calling `.apply(fontSizeFactor: 1.05)` directly on the geometry-less `.black` theme was guaranteed to fail on all 15 fields, every time, on every platform — this was never a Windows-specific or environment-specific bug, just one no prior round could have caught: nothing in this project had a compiler (rounds 1-7), and once CI existed (rounds 8-11) it only ran `flutter analyze`/`dart test` — static analysis has no opinion on a runtime assertion, and no widget test anywhere in the suite had ever rendered a widget tree under the real `AppTheme.light()`/`.dark()` (the one existing theme test, `app_theme_contrast_test.dart`, only checks `swatchFor()`'s plain `Color` math). **Fixed** by explicitly merging the matching geometry theme in first — `typography.black.merge(typography.englishLike).apply(fontSizeFactor: 1.05)` — so every field has a real, non-null `fontSize` before the factor is applied, matching the pattern Flutter's own `ThemeData.localize()` uses internally. Added `app/test/theme/app_theme_builds_without_crashing_test.dart`: pumps a real `MaterialApp` with each of `AppTheme.light()`/`.dark()` and a plain `Text` widget, asserts nothing throws (`tester.takeException()`), and separately asserts every core text style (`displayLarge` through `labelSmall`) still carries a non-null `fontSize` — not just that nothing crashed, but that the round-6 "high-contrast, generous, accessible text" intent this theme exists for wasn't silently lost by a future edit that drops the geometry merge without also re-adding `.apply()`.
- Also fixed, found while getting the project into a runnable state for the first time rather than by code review: this repo had never had a `windows/` platform folder committed (no one had run `flutter create --platforms=windows .` against it, because no round before this one had a working `flutter` toolchain at all to run it with) — `flutter run -d windows` fails outright with "No windows desktop project configured" until that scaffold exists. This isn't a code bug, just a missing one-time setup step; documented here so a future fresh clone doesn't hit the same confusing error blind. The generated `windows/` folder (CMake build files, the native Win32 runner shell, the default `.ico`) is pure Flutter-generated boilerplate with no project-specific logic in it, so it isn't a review target the way the rest of this codebase is.
- With the crash fixed, the app is confirmed to actually launch and render on a real Windows machine for the first time. Follow-up screens/flows have not yet been exercised by hand past initial launch — that's the natural next step, and any further findings from actually clicking through the app belong in a future round here rather than assumed now.
- **The same round then continued on to Android, on a real physical phone** (an Infinix X6528, Android 13) over USB, surfacing several more one-time environment issues distinct from the Windows ones — none of them code bugs, but each worth recording so a future fresh setup doesn't have to rediscover them blind:
  - Same root cause as the `windows/` folder gap above: this repo had never had an `android/` platform folder committed either, for the identical reason (no prior round had a working Android toolchain to generate one with). Fixed the same way, with `flutter create --platforms=android .`.
  - The Android SDK's command-line tools underwent a real, very recent (2026) transition — Google replaced the long-standing `sdkmanager` tool with a new `android` CLI (see https://developer.android.com/tools/agents/android-cli) — and Flutter (stable 3.47.0) hasn't fully caught up: `flutter doctor --android-licenses` silently no-ops instead of prompting for license acceptance, and `flutter doctor -v` kept reporting "Android license status unknown" regardless. Worked around by writing the standard Google-published license-acceptance hash files directly into `<sdk>/licenses/` (`android-sdk-license`, `android-sdk-preview-license`) — the same mechanism CI systems use to accept these licenses non-interactively — rather than waiting on an upstream fix.
  - Gradle's own auto-download of a project-required NDK version (triggered when the installed NDK doesn't match what `flutter.ndkVersion` expects) shells out to that same deprecated `sdkmanager.bat`, which on this machine crashed outright — Windows exit code `-1073740791` / `0xC0000409` (`STATUS_STACK_BUFFER_OVERRUN`) — rather than failing gracefully. **Fixed** in `app/android/app/build.gradle.kts` by pinning `ndkVersion` to a specific version string (`"30.0.15729638"`) confirmed already installed on the machine (via `Get-ChildItem "$env:LOCALAPPDATA\Android\sdk\ndk"`) instead of leaving it as `flutter.ndkVersion`'s default (`28.2.13676358`) — this app has no native C/C++ code of its own, so any reasonably recent installed NDK version satisfies whatever dependency's prebuilt library references it; which exact version is not a meaningful project decision. (On a later retry, Gradle's NDK auto-download did in fact succeed on its own — so the crash may have been transient, possibly disk-space-related, rather than fully deterministic — but the explicit pin is left in place regardless, since it removes a real class of failure with no downside.)
  - **A real, permanent, non-environmental fix**: the first real Gradle build failed with `:app:checkDebugAarMetadata` reporting "Dependency 'flutter_local_notifications' requires core library desugaring to be enabled" — a genuine, documented Android requirement (https://developer.android.com/studio/write/java8-support.html) for that plugin to use certain modern Java APIs on older Android versions, simply never triggered before because this was the first time the project had ever gone through a real Android build. Fixed in `app/android/app/build.gradle.kts`: `isCoreLibraryDesugaringEnabled = true` in `compileOptions`, plus a new `dependencies { coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.5") }` block (version checked against Google's Maven repository at the time, not assumed).
  - Also encountered and resolved as ordinary environment issues, not code bugs: the machine ran out of free disk space mid-build on the first Android attempt (Visual Studio + Android Studio + all SDK/Gradle downloads add up to well over the ~1 GB that was free at the time) — resolved by freeing space; and one build attempt hung indefinitely at `Running Gradle task 'assembleDebug'...` with 0% CPU/network activity in Task Manager — resolved by cancelling (Ctrl+C) and simply retrying, after which it proceeded normally.
  - **With all of the above resolved, SACRISTAN now runs on a real Android phone as well as real Windows desktop** — both of this project's two device-testable platforms (iOS would need a Mac, unavailable here) are now confirmed working outside any emulator or CI environment, for the first time in this project's history.
  - Note on what's preserved here versus what a fresh setup regenerates: the `android/app/src/main/res/**` launcher icons and `values*/styles.xml`, and `android/app/src/main/kotlin/.../MainActivity.kt`, are pure Flutter-generated boilerplate with zero project-specific content (identical to what `flutter create --platforms=android .` produces for any fresh Flutter project of this name) — like the `windows/` folder above, they aren't reviewed or hand-maintained here. Everything that *was* a real, hand-made fix this round (`app/build.gradle.kts`'s `ndkVersion` pin and desugaring block, plus the top-level Gradle config, wrapper, manifests, and `.gitignore`) is committed in full.

**Round 13** (this pass): with both testable platforms confirmed running, moved from "does it work" to "is it ready to submit" — starting on the two purely mechanical items from `docs/STORE_CHECKLIST.md`: a hosted privacy policy and a finished app icon. iOS/Mac-dependent work (TestFlight, on-device install) was explicitly dropped this round at the user's request — no Mac is available and none of the free/low-cost workarounds discussed (a personal Apple ID's 7-day install, GitHub Actions' free macOS CI minutes for a compile-only check) were pursued further.
- **Privacy policy**: published as a standalone page (not a project file) via the Artifact tool, so it has a permanent public URL suitable for both store listings' required privacy-policy field. Content matches the drafted copy already in `docs/STORE_CHECKLIST.md` (offline-only, nothing collected, `sacristan.sqlite` never leaves the device) and reuses the app's own seed color (`0xFF6A1B2B`) as its accent, so the page reads as the same product rather than a generic legal boilerplate page.
- **App icon**: the user supplied their own finished artwork (a navy/gold monogram — a stylized "S" incorporating a cross, chalice with host, thurible, and stole, ringed and captioned "SACRISTAN") in place of the round-6 placeholder chalice glyph that had been sitting at `assets/reference/app_icon_source.png`. Rather than use the supplied 1254×1254 PNG as-is, it needed real prep work before it was store-safe:
  - The source file had the wordmark baked in as pixels ("SACRISTAN" beneath the emblem) and a rounded-square white card baked in on a black canvas. Both are problems for app icon submission specifically: Apple's and Google's own icon guidelines advise against text in the icon itself (illegible at the smallest rendered sizes, and redundant with the app name the OS already draws under it), and a source image with its own rounded corners gets *double-masked* — the OS applies its own shape mask (circle, squircle, rounded-square, varying by launcher) on top of art that's already been cropped to one specific shape, which on a circular Android launcher would have clipped straight through the baked-in white card's corners into the black canvas around it.
  - **Fixed via image processing** (`icon_work/` scratch scripts, Python/Pillow — not committed, this project has no image-editing toolchain, so the work happened in the sandbox and only the outputs were kept): flood-filled the black canvas and white card background out to transparency (two-stage fill — the outer black first, then the interior white, since the interior white needed its own seed points to reach areas enclosed by the emblem's own linework), tightened to a bounding box of just the circular emblem (excluding the wordmark row entirely), then composited that cutout onto two fresh 1024×1024 layers: a full-bleed master icon (emblem centered on a flat white, matching the user's original card background — an initial maroon-background version, tying the icon to the app's own seed color, was built and offered first, but the user asked to keep it white) for `image_path`, and a second, more tightly-inset transparent-background layer for Android's *adaptive* icon foreground (content kept within roughly the inner 60% of the canvas, verified by simulating a circular mask over both the plain master icon and the composited adaptive layers — the plain master clips slightly at a full circle mask, the adaptive pair does not).
  - `pubspec.yaml`'s `flutter_launcher_icons` block gained `adaptive_icon_background: "#FFFFFF"` (matching `image_path`'s background) and `adaptive_icon_foreground: "assets/reference/app_icon_foreground.png"` alongside the existing `image_path`, so Android specifically builds a real two-layer adaptive icon instead of falling back to cropping the flat master image into whatever mask shape the launcher wants.
  - Checked legibility at small rendered sizes (48px, 24px) by actually downscaling the master icon, not just eyeballing the full-size version — the monogram, cross, and chalice stay readable at 48px; the thurible and stole (the emblem's smaller side elements) blur into soft gold shapes below that, which is expected and consistent with how similarly detailed icons from other apps degrade at extreme small sizes, not specific to this artwork.
  - **Found and fixed a real, pre-existing bug the moment the user actually ran the tool**: `pubspec.yaml`'s `flutter_launcher_icons` block had carried `ios: true` since long before this round (it was never touched by round 13's own edits), but this project has never had an `ios/` platform folder generated — iOS testing was dropped earlier this round, no Mac being available to create one with `flutter create --platforms=ios .`. Running `dart run flutter_launcher_icons` for the first time ever in this project's history (the round-6 placeholder icon had never been regenerated through this command before) crashed outright with `PathNotFoundException: ios/Runner/Assets.xcassets/AppIcon.appiconset/Icon-App-20x20@1x.png`, taking the whole run down with it before it ever reached the `windows:` block — Android's icons had already finished generating by that point (confirmed from the run's own log ordering), but Windows never got touched, either time the user ran it (crashed identically twice). Same shape as every other bug this project has found: latent for as long as no one had a working toolchain to actually run the command with. **Fixed** by setting `ios: false` in `pubspec.yaml`, with a comment explaining why and noting to flip it back if an `ios/` folder is ever generated. Confirmed fixed — the user re-ran the command and it completed successfully.
- **Windows installer**: added `installer/sacristan.iss`, an Inno Setup script that packages `flutter build windows --release`'s output into a real `SACRISTAN-Setup-<version>.exe` — Start Menu entry, optional desktop shortcut, a proper Windows uninstaller registration, and a per-user install path (`{localappdata}`) chosen deliberately over Program Files so installing doesn't need an admin password on a shared parish office computer. Deliberately not wired up: code signing (a paid certificate, ~$70-300/year — the script documents exactly where to plug a `SignTool` line in once one exists) and Microsoft Store/MSIX packaging (a separate Flutter-side toolchain, noted as a stretch goal in `docs/STORE_CHECKLIST.md`, independent of this installer). **Confirmed working end to end on the user's own machine**: `flutter build windows --release` succeeded (22s, incremental — the earlier 260s run was the first full build), the script compiled cleanly in Inno Setup ("Finished", ~17s, no errors — opened via the Start Menu app rather than double-clicking the `.iss` file directly, since file-association for `.iss` wasn't picking it up from Explorer), and the resulting `SACRISTAN-Setup-1.0.0.exe` installed and launched the app without issue (past the expected, unsigned-installer SmartScreen prompt). This is the first time this script has been run by anything other than this sandbox's static review of it — it was written blind, with no Windows/Inno Setup toolchain available here to test against, and it worked as designed on the first real attempt.
- **Support page**: published as a second standalone hosted page (Artifact tool), same design system as the privacy policy page (same color tokens, same Fraunces/Public Sans pairing) so the two read as one product rather than mismatched pages. Content is a short FAQ rather than a generic contact form, written from what's actually true about this specific app rather than boilerplate: covers offline-only operation, a link to the privacy policy, how to report a bug (what's actually useful to include), and the independence-from-any-diocese disclaimer already established elsewhere. One answer surfaced a real, previously-undocumented gap while writing it, not found by reviewing code: **there is no way to recover a forgotten Admin PIN**. Checked `ProfileRepository.verifyPin`/`setPin` (`repositories.dart`) to confirm rather than assume — the PIN is stored as a salted SHA-256 hash with no plaintext or recovery-question fallback anywhere, and since the app is deliberately offline/local-only (no server, no account), there's no remote channel a reset could even go through. The only current recourse is clearing the app's local data, which also erases all of that device's checklists and inventory, not just the PIN. Documented honestly on the support page (including advising sacristans to write the PIN down when first setting it) rather than glossed over or invented; **not fixed this round** — a real in-app recovery mechanism (e.g. a secondary recovery phrase set at PIN-creation time) is a genuine, scoped feature idea for a future round, the same "found, not fixed, needs a product decision" treatment given to `repeatRule`, `lowStockThreshold`, and template deletion in earlier rounds.
- **No-warranty disclaimer**: the user confirmed the app will be distributed free (not sold), and asked, after a legal-adjacent question about direct-download distribution outside the Microsoft Store, for a short as-is/no-warranty line — cheap insurance for software real sacristans will rely on for actual Mass prep, independent of any formal legal exposure. Added a new "No warranty" section to `AboutScreen` (`settings_screen.dart`), matching the existing sectioned layout (Privacy / Not an official publication / Attribution) rather than appending to an existing paragraph, and appended the same substance to the draft long-description copy in `docs/STORE_CHECKLIST.md` so it's visible before install, not just after. Deliberately scoped to these two existing surfaces rather than building a new standalone "download page," since none exists yet and none was asked for.
- **Play Store screenshots and feature graphic**: walked the user through capturing 5 phone screenshots (Today dashboard, an in-progress checklist, Inventory, Reference Library, Parish/Diocesan Calendar), checking Google's *current* published screenshot spec live (via `support.google.com`) rather than trusting the stale numbers already sitting in `STORE_CHECKLIST.md`, and confirmed the captured screenshots (720×1612 native) clear Google's hard minimum but sit below the 1080px+ recommended for premium placement eligibility — flagged, not blocking. Built the feature graphic (`store_assets/feature_graphic.png`, 1024×500, 24-bit PNG, no alpha — Google's exact spec) as an HTML page rendered through headless Chromium (Playwright, pre-installed in this sandbox) at 2x scale then downsampled with Lanczos resampling for crisp type, rather than hand-rasterizing — reuses the same roundel emblem cutout from the icon work (`icon_work/roundel_only.png`), the app's own maroon, and the Fraunces/Public Sans pairing already established across the privacy and support pages, so it reads as the same brand rather than a generic template. **First render had a real, visible bug**: the "SACRISTAN" wordmark at 92px overflowed past the 1024px canvas edge and got clipped mid-letter — caught by actually looking at the rendered output before delivering it, not assumed correct from the CSS. Fixed by resizing the emblem and rebalancing the layout (340px mark, 74px wordmark) so the text comfortably fits the available width with margin to spare; re-rendered and visually confirmed before delivery.
